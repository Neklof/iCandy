import styled from "styled-components";

const Cliente = () => {
	return <Componente />;
};

const Componente = styled.div`
	width: 100vw;
	height: 100vh;
	background: blue;
`;

export default Cliente;
